#include "main.h"
/**
 * _isalpha - compares if a letter is uppercase or lowercase
 *
 * @c: Takes character c arguement
 *
 * Return: Always 0
 */
int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (C >= 'A' && C <= 'Z'));
}
