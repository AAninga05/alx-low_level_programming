#include "main.h"
/**
 * _abs - program that prints out absolute value of interger
 *
 * @n: Integer parameter
 *
 * Return: Always 0
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (n * -1);
	}
	else
	{
	return (n);
	}
}
