#include "main.h"
/**
 * _abs - program that prints out absolute value of interger
 *
 * @int: Integer parameter
 *
 * Return: Always 0
 */
int _abs(int)
{
	if (n < 0)
	{
		n = -n;
		return (n);
	}
}
